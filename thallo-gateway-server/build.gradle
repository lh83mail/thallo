dependencies {
    compile project(':thallo-core')
    compile project(':thallo-server')
    compile 'org.springframework.cloud:spring-cloud-starter-zuul'

    compile 'org.springframework.security.oauth:spring-security-oauth2'
}
