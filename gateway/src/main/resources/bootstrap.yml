spring:
  application:
    name: gateway
  cloud:
    nacos:
      config:
        server-addr: ${NACOS_HOST:127.0.0.1:8848}
        file-extension: json
      discovery:
        server-addr: ${NACOS_HOST:127.0.0.1:8848}
        file-extension: json


management:
  endpoints:
    web:
      exposure:
        include: '*'