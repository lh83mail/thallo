server:
  port: 8888
spring:
  application:
      name: 'config-server'
  profiles:
    active: 'composite'
  cloud:
    config:
      server:
        composite:
          - type: native
            search-locations: 'file:///${user.home}/.spring-cloud/config-respo/'